name: CI

on: [push]

jobs:
    build:
        runs-on: [ubuntu-latest]

        steps:
            - name: Checkout Code
              uses: actions/checkout@v6.0.0

            - name: Lint Go Code
              uses: golangci/golangci-lint-action@v9

